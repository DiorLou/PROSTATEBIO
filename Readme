这是一个前列腺穿刺机器人的上位机测试程序

实验步骤：
1. 点击 "Connect", 连接上的同时会获取机械臂中"TCP_U","TCP_Tip","TCP_P"等定义
2. 打开 "Teach Mode On"按钮, 开启示教模式， 此举同时会先行切到 TCP_E, 然后打开示教
3. 点击 "Get A Point Position", 此举会换算出当前 Tip 在Base系下的位置，记作A 点，并添加至下拉列表变量 a_points_in_base_list 中, 在下拉列表中可以选择改变输入框 a_vars 的值
4. 点击 "Get O Point Position", 此举会换算出当前 Tip 在Base系下的位置，记作O 点，同时改变输入框 o_vars 的值
5. 点击 "Fine tune probe", 此举会切到 TCP_E (注意每次切换都会强制关闭示教模式), 然后记录当前点 TCP_E 在Base系下的位置，记作E 点，同时改变输入框 e_vars 的值。更新输入框 o_vars 的值以及 "TCP_O"的定义(注意此时会再次切换到TCP_E)
   
   
5. 点击 "Rotate the ultrasound plane to pass through the puncture point", 

点击左转x度时, 记录tcp_e_in_ultrasound_zero_deg 和 计算 volume_in_base, 然后点击 rotate_and_capture_2x, 采集完图像后, 点击 Rotate the ultrasound plane to pass through the puncture point,
这一步会更新 e 点, 转弯后, 更新 tcp_e_in_puncture_position. 然后点击 Read B Points in TCP_U from TXT File, 这一步会根据 tcp_e_in_ultrasound_zero_deg 来计算
Base 系下的 B 点, 更新完后, 会根据 tcp_e_in_puncture_position 以及 Base 系下的 B 点 来计算 需要绕 OA 旋转多少度， 计算每一个 B 点需要绕 OA 旋转的度数.
然后点击 Rotate the ultrasound plane to pass through the biopsy point, 这一步会根据列表中选中的 B point in base 以及当前姿态来计算下一步需要绕 OA 旋转的度数,


点击 ultrasound probe rotate left x Deg 的时候，所有选取的 a_points_in_base 会转化为 a_points_in_volume 然后导出到 A_points_in_volume.txt
超声平面旋转至B点时，会调用 _calculate_and_send_rcm_and_tcp_u_to_nav,
发送组合数据: "index(B点索引), x, y, z(RCM_in_Volume系), rx, ry, rz, rcm_x, rcm_y, rcm_z(TCP_U_in_Volume)"
倍福连接后开始轮询，穿刺机构的位置更新会同时 update_needle_pose_in_volume，
格式化并发送 针尖位姿(Volume系) 到导航服务器。发送格式示例: "UpdateNeedlePoseInVol,Nx,Ny,Nz,Nrx,Nry,Nrz;"

点击 "Read B Points (Volume) form Replan File" 的时候会把 biopsy_target_replan.txt 分割成 biopsy_target_replan_for_ax.txt

